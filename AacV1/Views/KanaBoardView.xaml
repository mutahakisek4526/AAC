<UserControl x:Class="AacV1.Views.KanaBoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="50音文字盤" FontSize="30" FontWeight="Bold" Margin="0,0,0,10" />
        <TextBox Grid.Row="1" Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" FontSize="28" Height="60" />

        <Grid Grid.Row="2" Margin="0,10,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ListBox Grid.Column="0" ItemsSource="{Binding KanaGroups}" SelectedItem="{Binding SelectedKanaGroup}" FontSize="26" />
            <ListBox Grid.Column="1" ItemsSource="{Binding Predictions}" FontSize="22" />
        </Grid>

        <WrapPanel Grid.Row="3">
            <Button Content="追加" Command="{Binding AddCharCommand}" Margin="4" />
            <Button Content="削除" Command="{Binding DeleteCommand}" Margin="4" />
            <Button Content="クリア" Command="{Binding ClearCommand}" Margin="4" />
            <Button Content="話す" Command="{Binding SpeakCommand}" Margin="4" />
            <Button Content="停止" Command="{Binding StopCommand}" Margin="4" />
        </WrapPanel>
    </Grid>
</UserControl>
