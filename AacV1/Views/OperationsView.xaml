<UserControl x:Class="AacV1.Views.OperationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="操作（AAC / PC操作 / 環境制御）" FontSize="28" FontWeight="Bold" />
        <ComboBox Grid.Row="1" ItemsSource="{Binding OperationModes}" SelectedItem="{Binding SelectedMode}" Margin="0,10,0,10" Width="200" />
        <ListBox Grid.Row="2" ItemsSource="{Binding EnvironmentActions}" SelectedItem="{Binding SelectedEnvironmentAction}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}" FontWeight="Bold" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding ActionType}" Margin="0,0,8,0"/>
                        <TextBlock Text="{Binding Argument}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <WrapPanel Grid.Row="3">
            <Button Content="Enter送信" Command="{Binding SendEnterCommand}" Margin="4" />
            <Button Content="左クリック" Command="{Binding MouseLeftClickCommand}" Margin="4" />
            <Button Content="環境制御実行" Command="{Binding ExecuteEnvironmentCommand}" Margin="4" />
        </WrapPanel>
    </Grid>
</UserControl>
