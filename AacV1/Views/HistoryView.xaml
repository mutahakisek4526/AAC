<UserControl x:Class="AacV1.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="履歴" FontSize="30" FontWeight="Bold" />
        <ListView Grid.Row="1" ItemsSource="{Binding HistoryItems}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="日時" DisplayMemberBinding="{Binding CreatedAt}" Width="220" />
                    <GridViewColumn Header="内容" DisplayMemberBinding="{Binding Text}" Width="600" />
                </GridView>
            </ListView.View>
        </ListView>
        <Button Grid.Row="2" Content="履歴クリア" Command="{Binding ClearHistoryCommand}" HorizontalAlignment="Left" Margin="4" />
    </Grid>
</UserControl>
